import { IngredientsState } from "@/types/ingredients";

const state = (): IngredientsState => ({
  all: []
});

const getters = {};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
