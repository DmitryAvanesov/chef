import { UnitsState } from "@/types/units";

const state = (): UnitsState => ({
  unitsList: [],
});

const getters = {};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
